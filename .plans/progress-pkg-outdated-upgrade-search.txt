## TASK-001 — 2026-06-11T18:00:00Z
Added `PkgAction::Outdated`, `PkgAction::Upgrade { name: Option<String> }`, and `PkgAction::Search { term: String }` to the clap enum in `src/main.rs`, wired stubs in `commands/pkg.rs`, and added disambiguation note to `haven upgrade` help text.
Files: ~src/main.rs, ~src/commands/pkg.rs, ~src/mise.rs, ~src/commands/list.rs
Decisions: Fixed three pre-existing `cargo clippy -D warnings` failures (dead_code in mise.rs, unnecessary_map_or and needless_option_as_deref in list.rs) to satisfy the clippy acceptance criterion.

## TASK-002 — 2026-06-11T18:30:00Z
Added `OutdatedPackage` struct and `brew_outdated`, `brew_upgrade`, and `brew_search` functions to `src/homebrew.rs`. `brew_outdated` parses `brew outdated --json=v2` JSON for both formulae and casks; `brew_upgrade` upgrades one or all packages; `brew_search` filters section headers from brew search output.
Files: ~src/homebrew.rs
Decisions: Used `#[allow(dead_code)]` on new items (matching `mise.rs` pattern) since callers land in later tasks. Noted that `brew outdated` exits 1 when packages are outdated — this is normal, so empty stdout with non-zero exit is the actual error signal.
