---
status: ACTIVE
---
# CEO Plan: dfiles — AI-First Developer Environment Manager

Generated by /plan-ceo-review on 2026-03-20
Branch: main | Mode: SCOPE EXPANSION
Repo: dfiles

---

## Vision

### 10x Check

The 10x version is not a better dotfiles manager. It's **the package registry for AI developer environments** — what npm was to Node, but for AI tool configurations.

Today: developers Google "best Claude Code skills" and manually install things from GitHub. There's no standard format, no versioning, no dependencies, no discovery. `skills.sh` is the closest thing but it's interactive and ephemeral.

The 10x version: you describe your environment declaratively, dfiles resolves dependencies,
fetches versioned packages, installs everything. You publish your curated setup as a package
others can install in one command. A thriving ecosystem emerges around it.

```
dfiles bootstrap gh:popular-author/rust-ai-env
# Installs: 8 Claude Code skills, /commit + /review commands, MCP servers,
#           rustup + cargo-watch + bacon, mise: rust@stable node@lts,
#           dotfiles: .zshrc .gitconfig .cargo/config.toml
# Time: under 2 minutes on a fresh machine. Nothing else does this.
```

### Platonic Ideal

Declarative AI environment composition: you describe what kind of developer you are,
not what files you want. The system resolves the right combination.

```toml
[identity]
type = "developer"
focus = ["rust", "typescript", "devops"]
ai_assistant = "claude"
style = "minimal"

[ai]
skills = ["gh:gstack/standard-skills", "gh:rust-lang/claude-rust-skills"]
```

The user specifies *intent*. dfiles figures out what that requires.

### Delight Opportunities

1. `dfiles status` — colored, emoji-rich, grouped by module. Like `git status` but satisfying.
2. `dfiles bootstrap --dry-run` — full install plan preview before touching anything.
3. Auto-generated `~/.claude/CLAUDE.md` — Claude Code learns what's installed.
4. `dfiles publish` — share your environment as a versioned GitHub release.
5. Shell completions — `dfiles completions --shell zsh` for all commands + profile names.
6. `dfiles log` — changelog of what changed across machines and when.
7. Homebrew tap — `brew install dfiles-sh/tap/dfiles` closes the bootstrap paradox.

---

## Scope Decisions

| # | Proposal | Effort | Decision | Reasoning |
|---|----------|--------|----------|-----------|
| 1 | Registry Protocol Standard | M | DEFERRED | Build the binary first; extract spec from real usage data |
| 2 | `dfiles publish` command | M | ACCEPTED | Social feature that creates virality; without it, dfiles stays personal |
| 3 | Auto-Generated CLAUDE.md | S | ACCEPTED | Low effort, high leverage — Claude Code becomes aware of its capabilities |
| 4 | Homebrew tap + curl install | S | ACCEPTED | The install story IS the product; closes the bootstrap paradox |
| 5 | Cross-machine diff | M | DEFERRED | Requires multiple machines; build after v1 is solid |
| 6 | Frictionless sync safety net | S | ACCEPTED | File watcher without .dfilesignore + blocklist is a footgun |

---

## Accepted Scope (added to this plan)

- `dfiles publish` — package config as versioned GitHub release (publishes to user's own repo, requires `gh auth`)
- Auto-generated `~/.claude/CLAUDE.md` on every `dfiles apply` (reads SKILL.md YAML frontmatter; profile noted but not conditional)
- Homebrew tap (`dfiles-sh/tap/dfiles`) + curl install script (`https://dfiles.sh/install`)
  - Requires: CI/CD pipeline (GitHub Actions) producing pre-built binaries for macOS x86_64/aarch64 + Linux x86_64/aarch64
- Safe file watcher with sensitive-file detection: `.dfilesignore` + built-in sensitive-file blocklist
  - Blocklist: `*.env`, `*.pem`, `*_rsa`, `*_ed25519`, `.npmrc`, `.pypirc`, `*.pfx`, etc.
  - `.dfilesignore` checked FIRST (stop processing if matched); blocklist only runs on unignored files
  - Confirmation prompt applies to both `dfiles watch` events AND `dfiles add` commands
  - Skipped files logged to `~/.dfiles/skipped.log` (JSON: `{"file": "...", "reason": "...", "ts": "..."}`)
- `dfiles bootstrap --dry-run` — show full install plan without executing
- Shell completions: `dfiles completions --shell [zsh|bash|fish]` (via `clap_complete` crate)
- `dfiles log` — changelog of tracked changes with timestamps

## Deferred to TODOS.md

- Registry Protocol Standard: define `dfiles.pkg.toml` format when real usage data informs the design
- Cross-machine diff (`dfiles diff --machine <name>`): implement once 2+ machines have state.json files
- Conflict resolution UX: three-way diff when `apply` finds diverged files (source ≠ live ≠ last-known-good)

---

## Updated Architecture (post-expansion)

### New Commands (from accepted scope)

```
dfiles init               # initialize new dfiles repo
dfiles bootstrap          # full setup: apply + packages + ai (NEW: installs dfiles too via tap)
dfiles bootstrap gh:user/env  # bootstrap from a published environment package
dfiles apply              # apply all modules to current machine
dfiles apply --profile minimal
dfiles apply --module ai
dfiles sync               # detect and record changes (explicit mode)
dfiles watch              # start file watcher (opt-in, with safety net)
dfiles status             # show drift — colored, emoji-rich output
dfiles diff               # diff between source and live files
dfiles add ~/.zshrc       # explicitly add a new file to tracking
dfiles upgrade            # update all GitHub-sourced AI configs
dfiles publish            # NEW: package current config as versioned GitHub release
dfiles publish --tag v1.0 --name "my-ai-env"
dfiles completions --shell zsh  # NEW: shell completions
dfiles log                # NEW: changelog of changes across time
```

### Safety Net Architecture

```
File Watcher Event
      │
      ▼
.dfilesignore check ──── IGNORED ──── stop
      │
      ▼
Sensitive-file blocklist
      │
      ├── MATCH ──── Prompt user: "Block or track?"
      │                    │
      │              User confirms ──── track with explicit annotation
      │              User declines ──── skip, log to ~/.dfiles/skipped.log
      │
      └── CLEAN ──── auto-track
```

### CLAUDE.md Generation

After every `dfiles apply`, dfiles reads the installed skills and commands (from their
metadata files) and writes/updates `~/.claude/CLAUDE.md`:

```markdown
# Claude Code Environment
Generated by dfiles on {date} (profile: work)

## Installed Skills
- /office-hours: YC Office Hours partner — brainstorm and design
- /ship: Release engineer — merge, test, push, create PR

## Slash Commands
- /commit: Create conventional commit with good message

## MCP Servers
- memory: Neo4j graph memory (neo4j://localhost:7687)
- filesystem: Local file access (~/)
```

Dfiles reads skill metadata from: `{skill_dir}/SKILL.md` YAML frontmatter
(name, version, description) and command metadata from `{commands_dir}/*.md`.

### `dfiles publish` Architecture

```
dfiles publish --tag v1.0 --name "my-ai-env"

1. Check: `gh auth status` — fail loudly if not authenticated
2. Check: does current git remote repo exist and user has write access?
   If no repo: prompt "Create github.com/{user}/dfiles-config? [y/N]"
   If no write access: fail with clear message + suggestion
3. Read current dfiles.toml + all module configs
4. Create manifest: dfiles-manifest.json
   { "name": "my-ai-env", "version": "v1.0",
     "profiles": [...], "modules": [...],
     "author": "jstegeman", "created": "2026-03-20" }
5. Bundle: source files + module configs + manifest
   ALWAYS excludes: secrets.toml, any file matching blocklist
6. Create GitHub release via gh CLI:
   gh release create v1.0 --title "my-ai-env v1.0" bundle.tar.gz
7. Print: "Published! Others can bootstrap with:"
          "  dfiles bootstrap gh:jstegeman/dfiles-config"
```

**Source notation:** `gh:{owner}/{repo}` resolves to latest release of `github.com/{owner}/{repo}`.
`gh:{owner}/{repo}@{ref}` resolves to a specific tag/commit. The `@` syntax is consistent across
all source types (skills, commands, full env packages).

### Homebrew Tap Structure

```
github.com/dfiles-sh/homebrew-tap/
  Formula/
    dfiles.rb       # formula: download release binary for current arch
```

Install script (`https://dfiles.sh/install`):
```bash
#!/bin/sh
# 1. Detect OS (macOS/Linux) + arch
# 2. Download pre-built binary from GitHub releases
# 3. Install to /usr/local/bin/dfiles (or ~/.local/bin on Linux)
# 4. Print: "dfiles installed! Run: dfiles bootstrap"
```

---

## Architecture Diagrams

### System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         dfiles CLI                              │
├──────────┬──────────┬──────────┬──────────┬────────────────────┤
│  init /  │ apply /  │ sync /   │ publish  │  bootstrap         │
│  add     │ status   │ watch    │          │  gh:user/env        │
└──────────┴──────────┴──────────┴──────────┴────────────────────┘
     │           │          │          │              │
     ▼           ▼          ▼          ▼              ▼
┌─────────┐ ┌────────┐ ┌────────┐ ┌────────┐  ┌──────────────┐
│ dfiles/ │ │ Module │ │ File   │ │ GitHub │  │ GitHub       │
│ config  │ │ Engine │ │ Watcher│ │ Release│  │ Source       │
│ source/ │ │        │ │+Safety │ │ via gh │  │ Fetcher      │
└─────────┘ └────────┘ └────────┘ └────────┘  └──────────────┘
               │                                      │
     ┌─────────┼─────────┬─────────┐                 │
     ▼         ▼         ▼         ▼                  ▼
┌──────────┐ ┌───────┐ ┌────────┐ ┌────────┐  ┌──────────────┐
│ Template │ │Homebrew│ │  AI   │ │Secrets │  │ dfiles.lock  │
│ Engine   │ │  mise  │ │Module │ │  1P    │  │ (SHA pins)   │
│ (Tera)   │ │        │ │CLAUDE │ │  op    │  └──────────────┘
└──────────┘ └───────┘ └───────┘ └────────┘
                           │
                    ┌──────┴──────┐
                    ▼             ▼
              ~/.claude/       ~/.dfiles/
              skills/          state.json
              commands/        skipped.log
              CLAUDE.md
```

### Bootstrap Flow

```
curl install | sh           OR    brew install dfiles-sh/tap/dfiles
      │                                         │
      └──────────────┬──────────────────────────┘
                     ▼
              dfiles bootstrap [gh:user/env]
                     │
          ┌──────────┴──────────┐
          │                     │
          ▼ (if gh:user/env)    ▼ (local config)
    Fetch + verify         Read dfiles.toml
    manifest from          + module configs
    GitHub release
          │                     │
          └──────────┬──────────┘
                     ▼
          Module dependency resolution
          (shell → git → packages → secrets → ai)
                     │
          ┌──────────┴──────────────────────┐
          ▼          ▼          ▼           ▼
    Symlink/     brew bundle  op://      GitHub
    copy files   install      secrets    skill fetch
                 mise install render
                     │
                     ▼
            CLAUDE.md generation
                     │
                     ▼
            state.json written
                     │
                     ▼
            dfiles status (show result)
```

---

## Open Questions (carry forward from design doc)

1. File naming: plain names + TOML dest mapping (preferred over chezmoi prefixes)
2. Lockfile format: `dfiles.lock` pins GitHub sources by commit SHA
3. **Conflict resolution: DECIDED** — default is backup-and-overwrite for MVP:
   `dfiles apply` backs up the live file to `~/.dfiles/backups/` before overwriting.
   Show user: "Conflict: ~/.zshrc backed up to ~/.dfiles/backups/zshrc.2026-03-20T12:00:00Z"
   Three-way diff tool deferred to post-v1 (TODOS.md).
4. Rust vs Go: language-agnostic design, Rust preferred
5. **NEW:** `dfiles publish` bundle format — tar.gz vs directory — and whether secrets.toml is excluded by default or opt-in exclude

---

## Strategic Positioning

```
  CURRENT TOOLS           WHAT THEY DO WELL         GAP
  ─────────────           ─────────────────         ────────────────────
  chezmoi                 dotfiles management        AI tooling, frictionless
  skills.sh               interactive skill install   managed/declarative
  mackup                  app settings backup        modern toolchain integration
  mise                    runtime version mgmt       environment bootstrapping
  Homebrew Bundle         package management         unified env bootstrap

  dfiles                  ALL OF THE ABOVE           + AI configs as packages
                          unified, declarative        + publish/share
                          one bootstrap command       + CLAUDE.md awareness
```

dfiles wins not by being better at any one thing but by being the only tool that
connects all of them — and adds the AI tooling layer that doesn't exist anywhere else.

---

## Temporal Interrogation

Implementation decisions to resolve before coding:

**HOUR 1 (foundations):**
- Which Rust crates? Recommend: `clap` (CLI), `tera` (templates), `toml` (config), `notify` (file watch), `reqwest` (HTTP for GitHub), `serde` (serialization)
- File structure: does `dfiles init` live inside a git repo (like chezmoi) or standalone directory?

**HOUR 2-3 (core logic):**
- How does apply handle files that already exist at the destination? Options: overwrite, backup, ask, fail.
- How does the symlink vs copy decision work? chezmoi copies; GNU Stow symlinks. Each has tradeoffs.

**HOUR 4-5 (integration):**
- 1Password subprocess: synchronous `op read` or batch? Batch is faster but requires waiting for all templates to be scanned first.
- GitHub rate limiting: unauthenticated API is 60 req/hour. For users with many gh: sources, this hits fast. Need `gh auth token` fallback.

**HOUR 6+ (polish/tests):**
- Shell completions: `clap_complete` crate handles this cleanly; wire it early.
- First-run experience: does `dfiles bootstrap` on a machine with no config ask questions or fail loudly?

---

## Feasibility Notes

- **1Password is optional.** If `op` CLI not installed, secrets module is skipped with a warning.
  Fallback: plain-text templates can use `{{ env "VAR_NAME" }}` as an alternative.
  No hard dependency on 1Password for core functionality.

- **GitHub API rate limiting.** Unauthenticated: 60 req/hour. Mitigation:
  (1) cache fetched GitHub sources in `~/.dfiles/cache/` with TTL;
  (2) prefer `gh auth token` when available;
  (3) batch all GitHub fetches at bootstrap start rather than inline.

- **Homebrew tap requires CI/CD.** GitHub Actions workflow to build and publish
  pre-built binaries for: macOS x86_64 (Intel), macOS aarch64 (Apple Silicon),
  Linux x86_64, Linux aarch64. This is boilerplate (`cross` crate + release workflow)
  but is ongoing maintenance. Estimate: 1 day to set up (human) / 15 min (CC).

- **SKILL.md YAML frontmatter format** (for CLAUDE.md generation):
  ```yaml
  ---
  name: office-hours
  version: 1.0.0
  description: "YC Office Hours partner — brainstorm and design"
  ---
  ```
  Fields `name` and `description` are required for CLAUDE.md generation.
  If missing or malformed, skill is listed with its directory name and "(no description)".
